add button to save image (and current settings) for debugging

runtime stats (recent and session; button to clear session stats)

view server log

setup mode vs. run mode.
* when transitioning out of setup mode, if a plate solution(s) were obtained, set
  the fov_estimate & distortion, and reduce the match_max_error to 0.002.
* sneak in a super-quick calibration series?
  * determine camera offset setting to avoid black crush at short exposures. Do
    this at entry into setup mode?

add affordance to set number of pattern_checking_stars

don't attempt solve if there are less than N detected stars (~10)

tetra3:
* report depth of pattern_checking_stars that were used for the match
* optimize use of patterns in pattern_checking_stars in overall brightness
  order

